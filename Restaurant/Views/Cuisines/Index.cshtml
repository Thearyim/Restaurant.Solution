<h1>Cuisines</h1>

@if (@Model.Count == 0)
{
<h3>No cuisines have been added yet!</h3>
}

<h3>Cuisine</h3>
@foreach (var cuisine in Model)
{
  <h4>@cuisine.GetName()</h4>
  <h4>@cuisine.GetTypeOfFood()</h4>
}
